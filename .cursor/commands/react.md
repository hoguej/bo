# /react

List iMessage tapback reactions via the bo CLI.

**Usage:** `/react` or `/react list`

Run: `npm run start -- react --list` from the project root.

Shows messages that have reactions. Sending reactions is not supported by the base bo CLI. Show the JSON output or a short summary.

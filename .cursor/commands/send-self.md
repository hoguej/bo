# /send-self

Send an iMessage to yourself via the bo CLI.

**Usage:** `/send-self <message text>`

Run: `npm run start -- send-self "THE MESSAGE TEXT"` from the project root.

The user's text after the command is the message body. Requires `BO_MY_PHONE` or `BO_MY_EMAIL` to be set. Show the CLI output or confirm success.
